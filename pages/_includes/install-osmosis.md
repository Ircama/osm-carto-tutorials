Osmosis is the application allowing to keep your database updated. To install it:

    echo 'yes' | add-apt-repository ppa:kakrueger/openstreetmap
    sudo apt-get update
    sudo apt-get --no-install-recommends install -y osmosis
